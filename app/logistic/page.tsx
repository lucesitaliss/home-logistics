import { auth } from "../../auth";

import Image from "next/image";

export default async function Home() {
  return (
    <main>
      <div className="bg-gray-200 flex justify-center items-center">Cuerpo</div>
    </main>
  );
}
